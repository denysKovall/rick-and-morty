export type Episodes = {
  name: string,
  id: string,
  episode: string,
  created: string,
  air_date: string,
  __typename: string,
  characters: [{ name: string, __typename: string,  id: string, image: string  }]
}
